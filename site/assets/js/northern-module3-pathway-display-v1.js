(()=>{'use strict';const frame=document.getElementById('activityFrame');let RELS=[];const norm=v=>String(v||'');async function ready(){try{const d=await window.BOOSTCareerRelationships?.load?.();RELS=d?.relationships||[]}catch(e){console.warn('Pathway display load failed',e)}}function memberships(soc){return RELS.filter(r=>norm(r.sourceSoc)===norm(soc))}function positionFor(soc,path){const r=RELS.find(x=>norm(x.sourceSoc)===norm(soc)&&x.pathway===path);return r?.position||''}function direct(from,to){return RELS.find(r=>norm(r.sourceSoc)===norm(from)&&(r.destinations||[]).some(d=>norm(d.soc)===norm(to)))}function describe(from,to){const d=direct(from,to);if(d){const tp=positionFor(to,d.pathway);return `${d.pathway} • ${d.position||'Current role'} → ${tp||'Related destination'}`;}const a=memberships(from),b=memberships(to);const same=a.find(x=>b.some(y=>y.pathway===x.pathway));if(same){const tp=positionFor(to,same.pathway);return `${same.pathway} • ${same.position||'Current role'} → ${tp||'Related pathway role'}`;}if(b.length){const t=b[0];return `${t.pathway} • ${t.position||'Mapped pathway role'} • No specific progression from your starting occupation is mapped; this is pathway context, not a promise of advancement.`;}return 'No specific mapped progression is available for this career yet. You can still keep exploring it using interests, preparation, transferable experience, and Northern regional evidence.'}function apply(){try{const d=frame.contentDocument,w=frame.contentWindow;if(!d||!w)return;const cur=w.__BOOST_NAZ_M3_API?.getCurrent?.();d.querySelectorAll('.boostM3CareerCompare[data-soc]').forEach(card=>{const soc=card.dataset.soc;const cell=[...card.querySelectorAll('.boostM3Evidence')].find(x=>x.querySelector('span')?.textContent?.trim()==='Career relationship');const b=cell?.querySelector('b');if(b)b.textContent=describe(cur?.soc,soc)});}catch(e){console.warn('Pathway display apply failed',e)}}async function bind(){await ready();const go=()=>{apply();try{const body=frame.contentDocument?.body;if(body&&!body.__boostPathObs){const o=new MutationObserver(()=>apply());o.observe(body,{childList:true,subtree:true});body.__boostPathObs=o}}catch(_){}};frame.addEventListener('load',()=>setTimeout(go,700));setTimeout(go,1600)}bind();})();